import { styled } from "styled-components";

export const StListWrapper = styled.ul`
  display: flex;
  flex-direction: column;
  gap: 1rem;
  padding: 1.5rem;

  h2 {
    font-size: medium;
    font-weight: 500;
  }
`;
